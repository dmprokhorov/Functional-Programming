#|
Прочитать трехмерный массив, содержащий вещественные числа, из стандартного потока ввода. Необходимо определить, какое число встречается в массиве чаще всего, и вывести
на экран количество вхождений.

Пример:
Дан трехмерный массив
#3A(((3.32 1.1 4.2 0.5) (7.7 3.33 1.1 0.5)) ((7.7 4.2 9.6 7.1) (12.12 0.03 4.2 0.03)) ((4.2 11.2 0.11 0.5) (8.03 4.2 1.1 2.77)))
Прочитать его из стандартного потока ввода можно с помощью следующей инструкции:
(defparameter arr (read))
Это массив размерности 3x2x4 (вспомните, как узнать размерность массива во время выполнения программы?). Чаще всего в нем встречается число 4.2 – количество вхождений
этого числа равно 5, этот результат и нужно вывести в стандартный поток вывода.

Подсказка: для того, чтобы определить, сколько раз в массиве встречается каждый элемент, можно использовать ассоциативные списки.
|#
(defparameter arr (read))
(defparameter alist NIL)
(defparameter dim (array-dimensions arr))
(defvar mx 0)
(dotimes (i (car dim))
  (dotimes (j (cadr dim))
    (dotimes (k (caddr dim))
      (let ((e (assoc (aref arr i j k) alist)))
        (if e
          (progn 
            (setf e (incf (cdr e)))
            (if (> e mx)
                (setf mx e)))
          (progn 
            (push (cons (aref arr i j k) 1) alist)
            (if (> 1 mx)
                (setf mx 1))))))))
(print mx)
(makunbound 'arr)
(makunbound 'alist)
(makunbound 'dim)
(makunbound 'mx)
